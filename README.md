# Emoneeds Stress Assessment - Local Setup

## Quick Start

```bash
npm install
npm run dev
```

Then open http://localhost:3000

## Notes
- This is a standalone JSX version (no TypeScript, no Supabase)
- Lead data is logged to console instead of being saved to a database
- To add backend storage, replace the console.log in App.jsx with your API call
